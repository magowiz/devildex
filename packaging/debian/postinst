#!/bin/sh
set -e
pip3 install --ignore-installed 'sqlalchemy>=2.0.41'
pip3 install --ignore-installed 'pdoc3>=0.11.6,<0.12.0'
pip3 install --ignore-installed 'markdownify>=1.2.0,<2.0.0'
pip3 install --ignore-installed 'pip-requirements-parser>=32.0.1,<33.0.0'
pip3 install --ignore-installed 'pydoctor>=25.4.0,<26.0.0'
pip3 install --ignore-installed 'fastmcp>=2.12.2,<3.0.0'
#DEBHELPER#
exit 0