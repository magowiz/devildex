#!/usr/bin/make -f
# See debhelper(7) (uncomment to enable)
# export DH_VERBOSE = 1

%:
	dh $@ --with python3

override_dh_auto_clean:
	# Do nothing to prevent dh_auto_clean from calling make distclean