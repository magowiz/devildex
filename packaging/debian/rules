#!/usr/bin/make -f
# See debhelper(7) (uncomment to enable)
# export DH_VERBOSE = 1

%:
	dh $@ --with python3

override_dh_auto_install:
	dh_auto_install
	poetry build --format wheel
	pip install --prefix=$(CURDIR)/debian/devildex/usr dist/*.whl