[pytest]
addopts = -n auto --dist=loadgroup
norecursedirs = docset
markers =
    focus: mark to execute only a single test
    ui: mark tests as UI related
    integration: mark tests as integration tests
    mcp_config: mark tests that configure MCP server behavior

[coverage:run]
omit =
    src/table_wx.py
